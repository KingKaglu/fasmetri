import { placeholderAdapter } from "@/server/scrapers/shops/placeholder";

// TODO: configure approved product/category endpoints. Do not scrape blocked query URLs.
export const veliAdapter = placeholderAdapter("veli", "Veli", "https://veli.store");
